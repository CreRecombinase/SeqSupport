#Utility functions for simulations, probably shouldn't be exported

# rssp_result_res <- function()
#expand(config["OUTPUT_DIR"]+"sim_chr{chrom}AF{AF}_{dataseta}_{datasetb}_{bdf}_{hm}_gwas_RSSp_res_{scen}_{pvv}_{lds}_{cf}_{doSplit}.txt.gz",
